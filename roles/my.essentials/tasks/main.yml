# apt-update
- import_tasks: tasks/apt-update.yml
  tags: apt-update

# apt-upgrade
- import_tasks: tasks/apt-upgrade.yml
  tags: apt-upgrade

# apt-install
- import_tasks: tasks/apt-install.yml
  tags: apt-install

# pip-install
- import_tasks: tasks/pip-install.yml
  tags: pip-install

# locales
- import_tasks: tasks/set-locales.yml
  tags: locales

# timezone
- import_tasks: tasks/set-timezone.yml
  tags: timezone

# adduser
- import_tasks: tasks/adduser.yml
  tags: adduser

# sudo
- import_tasks: tasks/sudoers.d.yml
  tags: sudo

# dotfiles
- import_tasks: tasks/dotfiles-install.yml
  tags: install-dotfiles
  when: install_dotfiles | default(False)

# lsd
- import_tasks: tasks/install-lsd.yml
  tags: install-lsd
  ignore_errors: true
  when: install_lsd | default(False)

# navi
- import_tasks: tasks/install-navi.yml
  tags: install-navi
  ignore_errors: true
  when: install_navi | default(False)

# bat
- import_tasks: tasks/install-bat.yml
  tags: install-bat
  ignore_errors: true
  when: install_bat | default(False)

# fd
- import_tasks: tasks/install-fd.yml
  tags: install-fd
  ignore_errors: true
  when: install_fd | default(False)

# btop
- import_tasks: tasks/install-btop.yml
  tags: install-btop
  ignore_errors: true
  when: install_btop | default(False)

# dategrep
- import_tasks: tasks/install-dategrep.yml
  tags: install-dategrep
  ignore_errors: true
  when: install_dategrep | default(False)

# glances
- import_tasks: tasks/install-glances.yml
  tags: install-glances
  ignore_errors: true
  when: install_glances | default(False)

# cht.sh
- import_tasks: tasks/install-cht.sh.yml
  tags: install-chtsh
  ignore_errors: true
  when: install_chtsh | default(False)

# lfs
- import_tasks: tasks/install-lfs.yml
  tags: install-lfs
  ignore_errors: true
  when: install_lfs | default(False)
