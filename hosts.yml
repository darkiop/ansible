# https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html
#
# https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html#connecting-to-hosts-behavioral-inventory-parameters
# --> ansible_ vars

all:

  hosts:
  
    #example.com:
  
  children:
    
    ## Proxmox: Cluster
    pve_cluster:
    
      hosts:

        pve01:
          ansible_host: 192.168.1.50
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve02:
          ansible_host: 192.168.1.51
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"
    
    ## Proxmox: Container & VMs
    pve_ct_vm:

      hosts:

        pve-ct-grafana:
          ansible_host: 10.4.1.3
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-mariadb:
          ansible_host: 10.4.1.7
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-bind-master:
          ansible_host: 10.4.1.9
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-bind-slave:
          ansible_host: 10.4.1.10
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-heimdall:
          ansible_host: 10.4.1.11
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-bitwarden:
          ansible_host: 10.4.1.12
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-iobroker:
          ansible_host: 10.4.1.20
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-ct-redis-1:
          ansible_host: 10.4.1.21
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-vm-cups:
          ansible_host: 10.4.1.31
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

        pve-vm-docker:
          ansible_host: 10.4.1.33
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"

    ## Raspberry PIs
    rpi:

      hosts:

        loki:
          ansible_host: 192.168.1.41
          ansible_connection: ssh
          ansible_user: "{{ vault_ansible_user }}"
          ansible_sudo_pass: "{{ vault_ansible_sudo_pass }}"
          ansible_ssh_private_key_file: "{{ vault_ansible_ssh_private_key_file }}"