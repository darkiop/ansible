- name: apt-install - install default software
  become: true
  become_user: root
  apt: 
    name: "{{ default_software }}"
    state: present
    update_cache: true

- name: apt-install - install aditional software
  become: true
  become_user: root
  apt: 
    name: "{{ aditional_software }}"
    state: present
    update_cache: true