# ansible-playbook books/reboot.yml -i hosts.yml -l pve_ct_vm
---
- hosts:
    - pve_hosts
    - pve_ct_vm
    - vpn_gw
  become: true
  become_user: root
  tasks:
    - import_tasks: ../tasks/reboot.yml